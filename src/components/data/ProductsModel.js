export class ProductsModel {
    constructor() {
        this.items = [];
    }
    getItem(id) {
        return;
    }
    ; // возвращает один товар для использования
}
// API.getItems() 
//  .then((data) => { 
//   ItemModel.items = data; 
//  })
